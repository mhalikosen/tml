<template>
  <section class="hero">
    <h1>{{ heading }}</h1>
    @if(subtitle)
      <p class="hero-subtitle">{{ subtitle }}</p>
    @end
  </section>
</template>

<style>
  .hero { padding: 4rem 2rem; background: linear-gradient(135deg, #1a1a2e 0%, #3040d0 100%); color: #fff; text-align: center; }
  .hero h1 { font-size: 2.5rem; margin-bottom: 0.5rem; }
  .hero-subtitle { font-size: 1.25rem; opacity: 0.85; }
</style>
